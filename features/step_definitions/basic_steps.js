var {defineSupportCode} = require('cucumber');

defineSupportCode(function({Given, When, Then}){
  Given("a {count} question survey", (count)=>{

  });

  When("I answer question {num}", (num)=>{

  });

  Then("I am returned question {num}", (num)=>{

  });
});